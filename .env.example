VITE_APPS_SCRIPT_URL=
# Chave do site reCAPTCHA v3 (App Check). Deixe em branco em dev/local — App Check
# só liga quando esta chave existe (build de produção). Gere no console do Google
# reCAPTCHA (tipo v3) e registre o app em Firebase Console → App Check.
VITE_RECAPTCHA_SITE_KEY=
